export default class circleRowConfigItem {
  constructor(category, image, coloursArray, name) {
    
    this.category = category;
    this.image = image; //this is now a URL remember
    this.colors = coloursArray,
    this.name = name
  }
}
